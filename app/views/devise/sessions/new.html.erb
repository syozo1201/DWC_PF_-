<div class="container">
  <div class="main">
    <div class="form-wrap">
      <div class="form-group text-center mb-4">
        <h2 class="mx-auto mb-4">お笑いびより</h2>
        <p>会員登録時に利用した方法で、ログインください。</p>
      </div>
      <%= form_with model: @user, url: user_session_path, local: true do |f| %>
        <%= devise_error_messages! %>

        <div class="form-group mb-4">
          <%= f.label "メールアドレスの入力", class: "font-weight-bold mb-0" %><br />
          <div class="mb-2">ご利用されているメールアドレスをご入力ください。</div>
          <%= f.email_field :email, autofocus: true, class: "form-control" %>
        </div>

        <div class="form-group mb-4">
          <%= f.label "パスワードの入力", class: "font-weight-bold mb-0" %><br />
          <div class="mb-2">半角英数字でご入力ください。</div>
          <%= f.password_field :password, autocomplete: "off", class: "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "ログインする", class: "btn btn-primary btn-shadow w-100" %>
        </div>
      <% end %>

      <br>

      <p class="devise-link">
        はじめてご利用の方ですか？
        <%= link_to "登録する", new_user_registration_path %>
      </p>
      <p>
        <%= link_to 'Twitterでログイン', user_twitter_omniauth_authorize_path, method: :post%>
      </p>
    </div>
  </div>
</div>
