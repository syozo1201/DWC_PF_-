<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/"><%= image_tag('logo.png') %></a>
  <% if user_signed_in? %>
    <div class="navbar-nav">
      <%= form_with url: search_path, method: :get, local: true do |f| %>
      <%= f.text_field :search , placeholder: 'キーワードを入力'%>
      <%= f.submit '検索', name: nil %>
      <% end %>
    </div>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-md-auto align-items-center">
      <% if user_signed_in? %>
        <li><%= link_to " 投稿する", new_post_path, class: "fas fa-plus-circle nav-link text-light" %></li>
        <li>
          <%= link_to user_path(current_user), {class:"nav-link text-light"} do %>
            <%= attachment_image_tag(current_user, :profile_image, fallback: "no-image.jpg", size:"25x25", class: "rounded-circle shadow-sm") %>
          <% end %>
        </li>
        <li style="margin-left:-10px">
          <%= link_to "マイページ", user_path(current_user), class:"nav-link text-light" %>
        </li>
        <li><%= link_to " 会員一覧", users_path, class: "fas fa-users nav-link text-light" %></li>
        <li><%= link_to " 投稿一覧", posts_path,class: "fas fa-camera nav-link text-light" %></li>
        <li><%= link_to " ログアウト", destroy_user_session_path, method: :delete, class: "fas fa-sign-out-alt nav-link text-light" %></li>
      <% else %>
        <li><%= link_to " トップページ", root_path, class: "fas fa-home nav-link text-light" %></li>
        <li><%= link_to " 会員登録", new_user_registration_path, class: "fas fa-user-plus nav-link text-light" %></li>
        <li><%= link_to " 会員様はこちら", new_user_session_path, class: "fas fa-sign-in-alt nav-link text-light" %></li>
      <% end %>
    </ul>
  </div>
</nav>