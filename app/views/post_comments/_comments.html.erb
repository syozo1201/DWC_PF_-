<!--<div class="comments">-->
  <!--<div>コメント件数：<%#= @post.post_comments.count %></div>-->
<% post.post_comments.each do |post_comment| %>
  <div class="row">
    <div class="col-3 text-center">
      <%= link_to user_path(post_comment.user_id) do %>
        <%= attachment_image_tag post_comment.user, :profile_image, fallback: "no-image.jpg", size:"60x60", class: "d-block mx-auto rounded-circle" %>
        <p class="small"><%= post_comment.user.name %></p>
      <% end %>
    </div>
    <div class="col-9">
      <p class="font-weight-bold mt-1"><%= post_comment.comment %></p>
      <p class="mt-2 mb-1"><%= post_comment.created_at.strftime('%Y/%m/%d') %></p>
      <% if post_comment.user == current_user %>
      <div class="comment-delete">
        <%= link_to post_post_comment_path(post_comment.post, post_comment), method: :delete, remote: true do %>
        <div class="fas fa-trash-alt no-underline-danger"></div>
        <% end %>
      </div>
      <% end %>
    </div>
  </div>
<% end %>